version: 2

models:
    - name: dim_zip_code_locations
      description: >
       Us zip_codes matched with their state, city, and county.
    - name: dim_movies_genres
      description: >
        unnested movie genres 
      columns:
        - name: movie_id 
          tests:
            - not_null
            - relationships:
                to: ref('stg_movies')
                field: movie_id
        - name: genre
          tests:
            - not_null           